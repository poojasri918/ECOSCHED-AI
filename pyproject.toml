[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecosched-ai"
version = "0.1.0"
description = "EcoSched AI OpenEnv project"
requires-python = ">=3.8"

dependencies = [
    "flask",
    "openai",
    "requests"
]